package com.krizaldis.homepulse.events

enum class EventType {
    TEMPERATURE_MEASURED,
    MOTION_DETECTED,
    DOOR_OPENED,
    DOOR_CLOSED,
    LIGHT_STATE_CHANGED,
    ENERGY_MEASURED,
    SMOKE_DETECTED
}